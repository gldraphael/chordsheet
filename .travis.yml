language: node_js
node_js:
  - "8"

script:
  - yarn lint
  - yarn test
  - yarn build

cache: yarn

deploy:
  provider: npm
  email: $EMAIL
  api_key: $AUTH_TOKEN
  on:
    tags: true
    branch: master